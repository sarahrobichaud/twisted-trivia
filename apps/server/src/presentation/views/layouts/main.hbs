<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{title}} - Trivia Quiz</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>

    <style>
        /* Custom styles */
        body {
            background-color: #f5f7fa;
            color: #333;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        }
        .brand-primary {
            background-color: #4b3bff;
        }
        .brand-secondary {
            background-color: #ff3b9a;
        }
    </style>
</head>
<body>
    <div class="max-w-screen-lg mx-auto p-4">
        {{> header}}
        <main class="py-4 min-h-[600px] border-t-2 border-b-2 border-gray-200 pt-4 my-8">
            {{#if isLobby}}
            <div class="flex justify-between items-center mb-6 border-b-2 border-gray-200 pb-4">
                <h2 class="text-2xl font-bold text-gray-800">Player Customization</h2>
                <input type="text" name="username" id="username" placeholder="Username" class="text-center py-2 px-4 text-2xl font-bold tracking-widest bg-white border-2 border-gray-300 rounded-xl focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent">
            </div>
            {{/if}}
            {{{body}}}
        </main>
        <footer class="py-4 mt-8 text-center text-gray-500 text-sm">
            <p>Networking for programmers - Sarah Robichaud and Dawn Wilson</p>
        </footer>
    </div>

    {{#if isGame}}
        <script type="module" src="/js/ws.js"></script>
    {{/if}}
</body>
</html> 